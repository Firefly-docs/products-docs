# 常见问题

## 默认登录账号是什么？

Debian 系统默认用户为 `firefly`，默认密码为 `firefly`。需要管理员权限时可执行 `sudo -s`。

## 如何确认 RK182X 模组已经识别？

先检查模组与主核心板的连接和供电，再参考 [RK182X 平台](ai_rk182x.md)中的环境检查与示例命令确认设备状态。

## 无法进入升级模式怎么办？

确认 `USB SEL` 拨码开关已切换到 `1`，使用质量良好的 Type-A 数据线连接 OTG 接口，然后在串口终端执行 `reboot loader`。